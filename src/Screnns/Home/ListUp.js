import * as React from "react";
import { View, Text } from "react-native";

const ListUp = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>ListUp Screen</Text>
    </View>
  );
};

export default ListUp;
