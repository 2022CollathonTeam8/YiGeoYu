import * as React from "react";
import { View, Text } from "react-native";

const MakeItem = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>MakeItem Screen</Text>
    </View>
  );
};

export default MakeItem;
